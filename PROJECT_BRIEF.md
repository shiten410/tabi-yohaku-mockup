# 旅行アプリ プロジェクト概要(検討まとめ)

他AI(Codex等)でのクロスチェック用に、これまでの検討内容を整理したもの。
最終更新:Codexによる設計レビュー(`CLAUDE_HANDOFF.md`)を反映済み。

## 確定事項(2026-09-17時点)

- **主DB:Cloudflare D1**(Supabaseは休眠リスクを理由に不採用)
- **地図:OpenFreeMap + MapLibreで自前ピン描画**(D1のPlaceデータを直接地図に表示。Googleマイマップ埋め込みは二重管理になるため不採用)
- **新規観光地の発見はアプリ外(Google Maps/SNS/ブログ)で行う前提**。アプリは「見つけた場所を保存・共有・整理する」役割に徹する。詳細確認・ナビはGoogle Mapsへdeep linkで橋渡し
- **位置情報取得フロー**:場所を保存する際にバックエンドで自動ジオコーディングを1回試行(Nominatim、レート制限順守)→失敗時は「地図未登録」と表示し、地図上をタップして手動でピン位置を確定
- **場所は5つのタグで管理**:気になる / 候補 / 行く予定 / 訪問済み / 今回はなし
  (「気になる」は思いつきレベルの軽いブックマーク、「候補」はある程度検討している状態、と2段階に分けて温度差を表現する)
  - タグによる絞り込みフィルタを一覧画面に設置
  - 地図のピン色をタグごとに色分け
  - 行きたい場所カードもタグごとに色分け(左端のアクセントカラー等)して一覧性を上げる
- 添付ファイル(フライトの予約書類等)の保存先は未確定(Vercel Blobが有力候補だが、カード登録なしで作成可能か要検証)
- **tabioriとは完全に別物として運用する(連携なし)**。そのため旅程(日程表)機能はこのアプリ自身が持つ
- **旅程は「今日の予定」だけでなく旅行全体の日ごとのタイムラインを持つ**(DayPlan/ScheduleItemでPlace・Flight・Lodgingを日付に紐付け)
- **やることリスト(準備TODO)を追加**:持ち物リストとは別に、予約・手続きなどの行動タスクを管理。担当者は未定でも登録可能にし、「担当未定だけ表示」で拾えていないタスクを可視化する
- **簡易認証は「名前+PIN」に決定**:旅行作成時に幹事がPIN(例:4桁)を設定。参加者は表示名+PINで参加し、以降は端末にセッションを保存(端末を変えた場合のみ再入力)。招待リンクを知らない人の侵入となりすましの両方をある程度防ぎつつ、パスワード管理ほど重くしない
- **アプリ名:「旅とも」に決定**
- **場所のタグ関連で追加検討中のアイディア**(未採用、候補として記録):
  - フィルタチップに件数バッジを表示(例:候補 3件)
  - 「気になる→候補→行く予定」とワンタップで格上げできるショートカット
  - 旅程タイムライン上の予定も場所タグと同じ配色に統一し、アプリ全体で一貫させる
  - 「今回はなし」をデフォルトで一覧から隠すオプション
  - 同じ場所に複数人が「気になる」を付けた場合に人数を表示(誰が気になっているか可視化。v2のリアクション機能と統合できそう)
- **Gemini API(無料枠、カード不要)を使ったAI機能を追加**。ただし「現在地から近い実在店舗をAIに新規発見させる」のはハルシネーションリスクが高いため不採用。AIは常に「アプリ内の保存済みデータ」か「一般知識の生成」の範囲に限定する

### 採用するAI機能(即効性が高いもの、全て採用)

| 機能 | 内容 | 精度への配慮 |
|---|---|---|
| うんちく・豆知識生成 | 保存した場所名からGeminiが背景・見どころを生成 | 「AI生成・事実と異なる場合あり」の注記を必ず表示 |
| 近場サジェスト+一言コメント | 現在地からの距離計算(Haversine、AI不使用)で保存済み候補地を近い順に並べ、Geminiが一言コメントを添える | 新規の場所を発見させず、保存済みPlaceデータの中だけで完結させる |
| 予約確認書の自動読み取り | フライト/ホテルのeチケットPDF・画像をGeminiの画像認識で読み取り、便名・日時・予約番号などをフォームに自動入力 | 読み取り結果は保存前に必ず本人が確認・修正できる画面を経由する |
| URLメモの自動下書き | 保存したURL先のテキストをGeminiが要約し、メモ欄の下書きを生成 | あくまで下書き。本人が確認・編集してから保存 |
| 持ち物リストのたたき台生成 | 行き先・季節・日数からGeminiが持ち物候補を生成 | **既存の持ち物リストと重複しない項目だけを提案**。提案は個別チェックボックスで選んで追加、または「すべて追加」ボタンで一括追加できるようにする(未確認のまま自動追加はしない) |
| やることリストのたたき台生成 | 海外/国内・日数などからGeminiが準備タスク候補を生成 | 上記と同様、既存タスクとの重複を避けた提案→個別選択/すべて追加のUIで確認してから追加 |

### AI提案の共通UIパターン(持ち物・やることリスト共通)

1. 「AIで◯◯リストの候補を作成」ボタンを押す(自動実行はしない、必ずボタン起動)
2. Geminiが提案リストを生成(**既存リストとの重複は除外**)
3. 提案は一覧でチェックボックス表示され、個別に選ぶか「すべて追加」で一括反映
4. 追加後は通常のリスト編集と同様に、誰でも編集・削除・担当者変更が可能

### AI機能は常にボタン起動(自動実行しない)

無料枠のレート制限を無駄に消費しないことと、ユーザーが意図せずAPIが呼ばれるのを防ぐため、
うんちく生成・近場サジェストを含む**すべてのAI機能はボタンタップ時にのみ実行**する。
ページを開いただけで自動的にAIが動く設計にはしない。

### AI生成した文言の保存・共有・再生成

- 場所の「豆知識」などAIが生成したテキストは、**生成した時点でD1に保存し、他の同行者にも同じ内容が見える**ようにする(生成者ごとに個別表示にはしない)
- 保存する文言には「生成日時・生成した人」を添えて表示し、誰かが既に生成済みなら再度APIを呼ばずにその保存済み内容を表示する
- 「再生成」ボタンを用意し、内容を更新したい場合はいつでも作り直せるようにする(再生成すると保存内容が新しいものに置き換わる)
- 「現在地から近い候補」はユーザーごとに現在地が異なるため全員共有の一意な正解にはならない。生成結果は参考情報として保存はするが、各自の現在地に応じて再生成することを前提としたUIにする

## 背景・課題

現在「tabiori」というアプリを使っているが、スケジュール管理などの基本機能は良い一方で、
**「+αの情報」を入力・共有しづらい**という課題がある。

具体的に不足している機能:
- 地図上で「行きたい場所」をメモできない
- そのURLを保存できない
- ホテルなど宿泊情報を入力できない
- 持ち物リストがない
- 飛行機の情報を入力できない

## 目的

同行者(友達)と共有して使える、上記の「+α情報」に特化した旅行アプリを作る。
tabioriを置き換えるのではなく、tabioriで弱い部分を補完する立ち位置。

## 絶対条件

- **無料で実現すること**(クレジットカード登録が必要な有料枠は避ける)
- 地図はGoogle Mapsの使い勝手を活かしたい(独自地図UIは作らない)

## 想定ユーザー・利用シーン

- 数人〜10人程度の友達グループでの旅行
- 全員が技術に詳しいわけではない前提(登録・操作のハードルを下げたい)
- **旅行前**:候補地のブレスト、持ち物準備、予約情報の入力
- **旅行中**:今日の予定・ホテル住所・フライト時刻をスマホでサッと確認
- 複数の旅行(今回だけでなく今後の旅行でも)使い続けたい

## 機能要件

### MVP(最初に作る)
- 招待リンクで参加(閲覧はログイン不要、編集は簡易認証)
- 行きたい場所リスト
  - メモ、URL、カテゴリ、ステータス(候補/行く予定/訪問済み/見送りの4段階)
  - Google Mapsへのディープリンク(緯度経度or住所からワンタップで開く)
- OpenFreeMap+MapLibreによる地図表示(D1のPlaceデータから直接ピン描画)
- ホテル情報(住所、チェックイン/アウト、予約番号)
- フライト情報(往復・乗継、添付ファイル)
- 持ち物リスト(共通/個人タブ、チェック状態を全員で共有)
- やることリスト(準備TODO):タスク名/担当者(未定可)/期限(任意)/完了状態、担当未定フィルタ
- 旅程(日ごとのタイムライン。Place/Flight/Lodgingを日付に紐付け、今日の日付を自動フォーカス)
- 複数旅行プロジェクトに対応(過去旅行のアーカイブも)

### 後回し(v2以降)
- 立て替え精算・割り勘計算
- リアクション/コメント機能
- プッシュ通知(新規追加の通知)
- 持ち物リストのテンプレート化
- オフライン対応の強化

## 地図の扱い方針(確定版)

Google Maps/Places APIは無料枠があってもAPIキー発行にクレジットカード登録(課金アカウント)が
必須なため採用しない。Googleマイマップ埋め込みも検討したが、埋め込み用のピンとアプリのPlace
データが同期されず二重入力になるため不採用とした。

1. **地図表示:OpenFreeMap + MapLibre**
   - APIキー不要・登録不要・完全無料の地図タイル
   - アプリのD1に保存したPlaceデータから直接ピンを描画(管理元を一本化、二重入力なし)
   - 店舗検索・口コミ・写真のデータベースは持たない(タイル表示のみ)
2. **新規の観光地探し・発見は引き続きGoogle Maps/SNS/ブログなどアプリ外で行う**
   - アプリはあくまで「見つけた場所を保存・共有・整理する」役割
3. **Google Maps Universal Link**:保存した場所の詳細確認・ナビはワンタップでGoogle Mapsへ。
   APIキー不要、無料。
   ```
   https://www.google.com/maps/search/?api=1&query=緯度,経度
   ```
   経路検索:
   ```
   https://www.google.com/maps/dir/?api=1&destination=...
   ```

### 位置情報の取得フロー(確定版)

「Google MapsのURLを貼れば自動で正確な座標が取れる」とは限らないため、以下の2段階で対応:

1. 場所を保存するタイミングでバックエンドが自動ジオコーディングを1回試行
   (Nominatim等、秒間1リクエストなどの利用規約を順守。自動補完的なリアルタイム検索には使わない)
2. 失敗した場合は「地図未登録」と表示し、OpenFreeMap地図上をタップしてピン位置を手動で確定できるようにする

この方式により、通常は自動で完結しつつ、取得できないケースも入力を止めずに救済できる。

## 画面構成案

```
旅行プロジェクト一覧(過去旅行も含む)
└─ 旅行ホーム
    ├─ 旅程(日ごとのタイムライン。今日の日付を自動フォーカス)
    │   └─ 各予定タップで詳細(場所メモ/ホテル住所/フライト時刻へ遷移)
    ├─ 行きたい場所リスト ⇄ 地図(OpenFreeMap、D1のPlaceから直接ピン描画)
    │   └─ 場所詳細(メモ/URL/ステータス/Google Mapsで開くボタン)
    ├─ 宿泊・交通
    │   ├─ ホテル一覧
    │   └─ フライト一覧
    ├─ 持ち物リスト(共通/個人タブ)
    ├─ やることリスト(担当者/未定フィルタ/期限/完了状態)
    └─ メンバー・共有設定(招待リンク発行)
```

## データ構造案

- `Trip`(旅行)── `Member`(参加者、名前+端末記憶ベースの簡易認証・案)
- `Trip` 1:N `Place`(名前/メモ/URL/緯度経度 or 住所/タグ5種(気になる・候補・行く予定・訪問済み・今回はなし)/カテゴリ/`aiTrivia`・`aiTriviaGeneratedAt`・`aiTriviaGeneratedBy`)
- `Trip` 1:N `Flight`
- `Trip` 1:N `Lodging`
- `Trip` 1:N `PackingItem`(共通/個人フラグ、担当者、チェック状態)
- `Trip` 1:N `Task`(やることリスト:タスク名/担当者(未定可)/期限/完了状態)
- `Trip` 1:N `DayPlan`(日付、メモ) 1:N `ScheduleItem`(時刻、種別、Place/Flight/Lodgingへの参照 or 自由記述)

## 技術スタック(検討中・すべて無料枠)

| 用途 | 候補 | 備考 |
|---|---|---|
| 主DB | **Cloudflare D1(確定)** | クレカ不要、DB 500MB/アカウント合計5GB、休眠による自動停止の記載なし |
| ホスティング・API | Cloudflare Workers、またはVercel+Workers API | 未確定。維持管理負担とCORS/認証設計を比較して決定 |
| 地図 | OpenFreeMap + MapLibre + Google Maps Universal Link | クレカ登録不要、二重入力なし |
| 添付ファイル(フライト予約書類等) | 未確定(Vercel Blobが有力候補) | カード登録なしで作成できるか要検証 |
| PWA化 | フレームワーク標準機能 | ホーム画面追加、スマホ操作前提のUI |
| OGP取得(URL先のタイトル/画像) | 自前のサーバーレス関数でfetch+パース | 外部有料API不使用、取得失敗でもURL保存は妨げない |
| AI機能 | Gemini API(無料枠) | カード登録不要。レート制限(RPM/RPD)があるため、個人利用規模なら問題ないが乱用しない設計にする |

**注意点**:
- D1の無料枠(読み取り500万行/日、書き込み10万行/日など)を超えるとエラーになるため、失敗表示の設計が必要
- Nominatimは秒間1リクエストなどの利用規約を厳守し、自動補完的な使い方はしない
- Vercel Blobなど添付ファイル用サービスがカード登録不要か、実際に検証してから確定する

## 未確定・要検討事項

- フレームワークの最終選定(Next.js想定だが未確定)
- 簡易認証の実装方式(名前入力のみで良いか、パスコードを設けるか)
- マイマップとアプリ内Placeデータの紐付け方(手動対応で割り切るか、URLだけ持たせるか)
- 通知の代替手段(プッシュ通知を実装しない場合、更新差分をどう見せるか)
